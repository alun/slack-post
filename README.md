# Running

Intall `npm i` `npm i -g pm2`

Create .envrc file containing following vars:

    export TOKEN=slack-bot-token
    export PORT=http-server-port
    export USER=slack-user-to-send-messages-to

Run with `./run.sh`

